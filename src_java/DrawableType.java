package pixel_tracer.src_java;

/**
 * Liste des categories de formes manipulees par le systeme.
 */
public enum DrawableType {
    /** Point 2D. */
    POINT, LINE, SQUARE, RECTANGLE, CIRCLE, POLYGON, CURVE
}
